@kernel
def kernely():
	d = 5
	for y=0 to 100:
		e= inputA[y]
		d += a
	
	output = d

@kernel
def kernelz():
	c = 5
	for y=0 to 100:
		a = inputA[y]
		b = inputB[y]
		c += a * c
	output = c
	
kernely()
kernelz()